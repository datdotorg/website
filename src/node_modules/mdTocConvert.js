const unified = require('unified')
const markdown = require('remark-parse')
const html = require('remark-html')
const contents = require("remark-contents")

function mdTocConvert() {
    return unified()
            .use(markdown)
            .use(contents)
            .use(html)
}

module.exports = mdTocConvert